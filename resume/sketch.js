function setup() {
  createCanvas(windowWidth, 150);
  background('#f0f0f0');
  textAlign(CENTER, CENTER);
  textSize(24);
  fill(50);
  // text("Welcome to My Resume Page", width / 2, height / 2);
}

function windowResized() {
  resizeCanvas(windowWidth, 150);
}
